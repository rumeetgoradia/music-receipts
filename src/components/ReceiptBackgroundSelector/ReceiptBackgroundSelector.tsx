import { StaticImageData } from "next/image";

type ReceiptBackgroundSelectorProps = {
	setReceiptBackground: (receiptBackground: StaticImageData) => void;
};

const ReceiptBackgroundSelector: React.FC<
	ReceiptBackgroundSelectorProps
> = ({}) => {
	return <></>;
};

export default ReceiptBackgroundSelector;

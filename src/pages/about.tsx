import { Layout } from "@/components/Layout";
import { NextPage } from "next";

const AboutPage: NextPage = () => {
	return <Layout title="About">Hello. this is about</Layout>;
};

export default AboutPage;
